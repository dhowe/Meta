
// Do a search on Google news 

String api = "https://ajax.googleapis.com/ajax/services/search/news?v=1.0&q=";
String query = "Barack Obama";

String url = api + query.replaceAll(" ","+");
JSONObject json = loadJSONObject(url);
JSONObject rData = json.getJSONObject("responseData");
JSONArray results = rData.getJSONArray("results");

for (int i = 0; i < results.size(); i++) 
  println(i+") "+results.getJSONObject(i).getString("content")+"\n");
