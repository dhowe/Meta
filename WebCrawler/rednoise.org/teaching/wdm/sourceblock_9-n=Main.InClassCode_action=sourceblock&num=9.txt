
// Our very simple example from class (use Processing's editor)
// To run, push Processing's 'play' button 

float ypos = 50;

void setup() {

  size(600,600);  
}

void draw() {
  background(40,60,90);
  textSize(36);
  text("Godzilla was in the ", 50, 50);
  text("fountain", 400, ypos);
  ypos++;
}
