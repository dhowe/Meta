
// Simple timer example, change background every 2 sec

float now, bgcol=100;

void draw(){

  background(bgcol);

  if (millis()-now > 2000) { // has 2 sec elapsed?

    bgcol = random(0,256); // change the bg color

    now = millis(); // reset the counter
  }

}
